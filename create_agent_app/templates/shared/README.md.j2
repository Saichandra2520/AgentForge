# {{ project_name }}

{{ agent_description }}

Built with [create-agent-app](https://github.com/Saichandra2520/agentforge) —
Vite-style scaffolder for production Agentic AI projects.

## Setup

1. Install dependencies
   pip install -r requirements.txt

2. Configure environment
   cp .env.example .env
   # Edit .env and add your API keys

3. Configure the agent
   # Edit config.yaml to customise behaviour

4. Run the agent
   python main.py

## Customisation

See CUSTOMISATION.md for a guide to changing any part of this project.

## Template

- Agent type: {{ template }}
- LLM provider: {{ llm_provider }}
{% if include_api %}- API: FastAPI on http://localhost:8000{% endif %}
{% if include_tests %}- Tests: pytest tests/{% endif %}